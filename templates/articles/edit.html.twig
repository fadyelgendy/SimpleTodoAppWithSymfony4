{% extends 'base.html.twig' %}

{% block title %} Update Todo {% endblock %}

{% block body %}
    <div class="container">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}

        <a href="{{ path('todo_list') }}" class="btn btn-dark mt-3 rounded">Cancel</a>
    </div>
{% endblock %}